# Note: template strings such as %TOFU_VERSION% shall be replaced when
# build.sh is run. The only storage place where the version numbers should
# be contained is within build.sh where it is independent of the actual
# code.
TOFU = "%TOFU_VERSION%"
